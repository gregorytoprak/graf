{
  "name": "graph-maker",
  "version": "0.1.0",
  "homepage": "http://graph.gdt.io/",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "deploy": "react-scripts build && surge ./build && open http://graph.gdt.io/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
